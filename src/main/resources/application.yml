spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dv
    username: app
    password: telus
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: tournament
